package com.codedirect.laundry.utils.dummies

import com.codedirect.laundry.utils.models.Menus

object DataMenu {
    fun getData(): ArrayList<Menus> {
        val menus = arrayListOf<Menus>()
        return menus
    }
}